{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer un tag</h1>
    <table class="table table-bordered">
        <thead>
        <tr>
            <th>id</th>
            <th>email</th>
            {#<th>Update</th>#}
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                {#<td>
                        <a href="{{ path('app_tag_update', {id: user.id}) }}" class="btn btn-warning">Modifier</a>
                        <a href="{{ path('app_tag_delete', {id: user.email}) }}" class="btn btn-danger">Supprimer</a>
                    </td>
                #}
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
